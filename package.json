{
  "name": "m2-hyva-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "dynamic layered magento 2 test environment",
  "workspaces": [
    "src/apps/*"
  ],
  "scripts": {
    "env-check": "APP_NAME=pps node scripts/verify-browsers.js",
    "test:all": "yarn --silent env-check && yarn --silent workspace pps run test:all",
    "test:all:ui": "yarn --silent workspace pps run test:all:ui",
    "test:admin": "yarn --silent env-check && yarn --silent workspace pps run test:admin",
    "test:pps-admin": "yarn --silent env-check && yarn --silent workspace pps run test:pps-admin",
    "test:hyva": "yarn --silent env-check && yarn --silent workspace pps run test:hyva",
    "test:pps": "yarn --silent env-check && yarn --silent workspace pps run test:pps",
    "test:checkout": "yarn --silent env-check && yarn --silent workspace pps run test:checkout",
    "test:debug": "yarn --silent env-check && yarn --silent workspace pps run test:debug",
    "test:ui": "yarn --silent workspace pps run test:ui",
    "test:display": "yarn --silent env-check && yarn --silent workspace pps run test:display",
    "report": "yarn --silent workspace pps run report"
  },
  "author": "Lucas van Staden",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^9.0.0",
    "@playwright/test": "1.58.1",
    "adm-zip": "^0.5.16",
    "axios": "^1.6.2",
    "colors": "^1.4.0",
    "playwright": "1.58.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3",
    "yarn": "^1.22.19"
  }
}
